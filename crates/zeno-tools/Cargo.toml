[package]
name = "zeno-tools"
version = "0.1.0"
edition = "2024"
description = "Tool trait definitions and shared types for Zeno agent tools"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
async-trait = "0.1"
thiserror = "2"
